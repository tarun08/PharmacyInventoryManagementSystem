import { ExpiryColorPipe } from './expiry-color-pipe';

describe('ExpiryColorPipe', () => {
  it('create an instance', () => {
    const pipe = new ExpiryColorPipe();
    expect(pipe).toBeTruthy();
  });
});
