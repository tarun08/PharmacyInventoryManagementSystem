import { InverntoryColorPipePipe } from './inverntory-color-pipe-pipe';

describe('InverntoryColorPipePipe', () => {
  it('create an instance', () => {
    const pipe = new InverntoryColorPipePipe();
    expect(pipe).toBeTruthy();
  });
});
